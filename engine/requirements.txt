# MultiBot Engine dependencies.
#   uv venv .venv --python 3.12
#   uv pip install --python .venv/Scripts/python.exe -r requirements.txt
#
# hermes-agent is not pinned here: its backend may reject wheel/sdist
# ("Building wheels or sdists for hermes-agent is not supported"), działa
# wyłącznie editable. Instalacja (SHA: 17688f9):
#   git config --global core.longpaths true   # Windows: MAX_PATH during clone
#   git clone --filter=blob:none https://github.com/NousResearch/hermes-agent
#   git -C hermes-agent checkout 17688f9
#   uv pip install --python .venv/Scripts/python.exe -e ./hermes-agent
aiohttp
fastapi
uvicorn
httpx
pytest
playwright
mcp==1.28.1
starlette==1.3.1
numpy
edge-tts
python-multipart
